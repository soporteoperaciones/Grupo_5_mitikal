<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fontawesome.com/icons?d=gallery&p=2&m=free">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@300&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Mulish:wght@600&family=Rubik:ital,wght@1,300&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Mulish:wght@600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles-login.css">
    <title>Mitikal</title>
</head>

<body>
    <%- include("../partials/header") %>
        <main>
            <div class="menu">

            </div>
            <div class="box-register">
                <h1>INICIAR SESIÓN</h1>
                <form action="/users/login" method="POST">
                    <div class="input-container">
                        <label for="email">E-Mail</label>
                        <input type="text" name="email" id="email" class="<%= (locals.errors && errors.email) ? 'input-error' : '' %>" value="<%= locals.oldValues && oldValues.email %>" />
                        <% if (locals.errors && errors.email) { %>
                            <p class='msg-error'>
                                <%= errors.email.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="input-container">
                        <label for="password">Contraseña</label>
                        <input type="password" name="password" id="password" class="<%= (locals.errors && errors.password) ? 'input-error' : '' %>" value="<%= locals.oldValues && oldValues.password %>" />
                        <% if (locals.errors && errors.password) { %>
                            <p class='msg-error'>
                                <%= errors.password.msg %>
                            </p>
                            <% } %>
                    </div>

                    <button class="iniciar_sesion" type="submit">INICIAR SESIÓN</button>
                    <div class="crear-cuenta">¿No tenés cuenta aún? <a href="/register">Crear Cuenta</a></div>
                </form>
            </div>
        </main>



        <%- include("../partials/footer") %>

</body>

</html>