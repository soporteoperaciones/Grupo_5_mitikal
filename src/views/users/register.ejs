<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script type="text/javascript" src="/js/check-password.js"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fontawesome.com/icons?d=gallery&p=2&m=free">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@300&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Mulish:wght@600&family=Rubik:ital,wght@1,300&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Mulish:wght@600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles-register.css">
    <title>Mitikal</title>
</head>

<body>



    <%- include("../partials/header") %>

        <main>
            <div class="box-register">
                <h1>CREAR CUENTA</h1>
                <H2>Comprá más rápido y llevá el control de tus pedidos, ¡en un solo lugar!</H2>
                <form action="/users/register" name="register-form" method="POST" enctype="multipart/form-data">
                    <div class="input-container">
                        <label for="name">NOMBRE</label>
                        <input type="text" name="name" id="name" class="<%= (locals.errors && errors.name) ? 'input-error' : '' %>" value="<%= locals.oldValues && oldValues.name %>" />
                        <% if (locals.errors && errors.name) { %>
                            <p class='msg-error'>
                                <%= errors.name.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="input-container">
                        <label for="email">E-Mail</label>
                        <input type="email" name="email" id="email" class="<%= (locals.errors && errors.email) ? 'input-error' : '' %>" value="<%= locals.oldValues && oldValues.email %>" />
                        <% if (locals.errors && errors.email) { %>
                            <p class='msg-error'>
                                <%= errors.email.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="input-container">
                        <label for="tel">TELÉFONO</label>
                        <input type="tel" name="tel" id="tel" class="<%= (locals.errors && errors.tel) ? 'input-error' : '' %>" value="<%= locals.oldValues && oldValues.tel %>" />
                        <% if (locals.errors && errors.tel) { %>
                            <p class='msg-error'>
                                <%= errors.tel.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="input-container">
                        <label for="password1">Contraseña</label>
                        <input type="password" name="password1" id="password1" class="<%= (locals.errors && errors.password1) ? 'input-error' : '' %>" value="<%= locals.oldValues && oldValues.password1 %>" />
                        <% if (locals.errors && errors.password1) { %>
                            <p class='msg-error'>
                                <%= errors.password1.msg %>
                            </p>
                            <% } %>
                    </div>
                    <div class="input-container">
                        <label for="password2">Confirmar Contraseña</label>
                        <input type="password" name="password2" id="password2" class="<%= (locals.errors && errors.password2) ? 'input-error' : '' %>" value="<%= locals.oldValues && oldValues.password2 %>" />
                        <% if (locals.errors && errors.password2) { %>
                            <p class='msg-error'>
                                <%= errors.password2.msg %>
                            </p>
                            <% } %>
                    </div>

                    <div class="input-container">
                        <label for="file-upload" class="subir">
                            <i class="fas fa-cloud-upload-alt"></i> Subir archivo
                        </label>
                        <input id="file-upload" onchange='cambiar()' type="file" style='display: none;'/>
                        <% if (locals.errors && errors.image) { %>
                            <p class='msg-error'>
                                <%= errors.image.msg %>
                            </p>
                            <% } %>

                        
                        

 

                    </div>

                    <div class="iniciar-sesion">¿Ya tenés una cuenta? <a href="/users/login">Iniciá Sesión</a></div>
                    <button class="crear-cuenta" type="submit">CREAR CUENTA USUARIO</button>

                </form>


                


            </div>
        </main>





</body>

</html>